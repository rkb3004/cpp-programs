#include <bits/stdc++.h>
using namespace std;

int main(){
    char a[100];
    cin.getline(a, 100); // Read a line of input into the array
    cout << a << endl; // Output the string stored in the array
}